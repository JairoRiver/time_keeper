package pages

import "github.com/JairoRiver/time_keeper/internal/view/layout"

// F3.1 — implement the landing hero content here.
templ Landing() {
	@layout.Base("TimeKeeper") {
		@layout.Header(false)
		<main class="flex-1 flex items-center justify-center p-10">
			<p class="text-gray-400">Landing — próximamente (F3.1)</p>
		</main>
	}
}
